// Configuration for the Inventory module (Step 6). Kept separate from the
// store/UI so real settings could later replace these constants.

export const COMMON_UNITS = ["pcs", "box", "pack", "set", "roll", "ream"];

export const CURRENT_DEMO_USER = "Mommy Ann";
